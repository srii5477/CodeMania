<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap">
    <style>
        body {
            background: repeating-linear-gradient(to right, rgb(43, 168, 31), rgb(0, 157, 255));
            color: rgb(17, 3, 86);
            font-family : 'Poppins', sans-serif;
            margin: 5%;
        }
    </style>
</head>
<body>
    <h1 style="margin-left: 5%;"> Welcome to the Onboarding Page for your Test.</h1>
    <% if(locals.warning) { %>
        <%= warning %>
    <% } %>
    <% if(locals.qs) { %>
        <% for(let i = 0; i < qs.length; i++) { %>
            <form action="/run-code" method="POST"  autocomplete="off">
            <div class="qcard" style="margin: 2%; padding: 2%;">
                <h2>Q <%= i+1 %>: </h2>
                <h3><b><%= qs[i].title %></b></h3>
                <h4><%= qs[i].question %></h4>
                <h5><%= qs[i].examples %></h5>
                <h4><%= qs[i].difficulty %></h4>
                <h4> Credits: <%= qs[i].credits %></h4>
                <h5>Tags: [<%= qs[i].tags %>]</h5>
            </div>
            <input type="hidden" name="qtitle" value="<%= qs[i].title %>"/>
            <input type="text" style="margin-left: 7%; width: 73.7%;" name="language" placeholder="Enter the language you are using: We support Python, Java, CPP, C, JavaScript, among many others." autofocus/>
            <textarea rows="20" cols="60" name="submission" style="margin-left: 7%;" spellcheck="false"></textarea>
            <button type="submit" class="btn btn-outline-light btn-lg" style="margin-top: 3%; margin-left: 40%;"> Run code </button>
        </form>
        <% } %>
    <% } else { %>
        <h4> No results! </h4>
    <% } %>
</body>
</html>